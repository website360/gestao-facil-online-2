import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Alert, AlertDescription } from '@/components/ui/alert';
import { Info, Package, Scale, Ruler } from 'lucide-react';

const CorreiosIntegrationInfo = () => {
  return null; // Informações movidas para o cadastro de produtos
};

export default CorreiosIntegrationInfo;